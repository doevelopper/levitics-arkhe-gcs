#ifndef @NAMESPACE@EXPORT_HPP
#define @NAMESPACE@EXPORT_HPP

	#include <QtGlobal>

	#if defined(Q_OS_WIN)
	#  if defined(@NAMESPACE@_API_EXPORTS)
	#    define @NAMESPACE@_EXPORT Q_DECL_EXPORT
	#  else
	#    define @NAMESPACE@_EXPORT Q_DECL_IMPORT
	#  endif
	#endif

	#if !defined(@NAMESPACE@_EXPORT)
		#if defined(BUILD_SHARED)
			#define @NAMESPACE@_EXPORT Q_DECL_EXPORT
		#else
			#define @NAMESPACE@_EXPORT
		#endif
	#endif

#endif

