#ifndef EXCEPTIONS_HPP
#define EXCEPTIONS_HPP

#include <clocale>
#include <stdexcept>

class Exceptions 
{
public:
    Exceptions();
    Exceptions(const Exceptions& orig);
    virtual ~Exceptions();
private:

};

#endif

